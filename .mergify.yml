commands_restrictions:
  queue:
    conditions:
      - sender = onecursor5-design

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = BB_CHECKBOX_NEVER_GATE_20260811_91E2
    merge_method: merge

merge_queue:
  queue_controls_comment: true
  status_comments: all
