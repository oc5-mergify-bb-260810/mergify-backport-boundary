pull_request_rules:
  - name: BB copy provenance positive
    conditions:
      - title = BB copy naming positive
    actions:
      copy:
        branches:
          - release-bb
        title: "BB copy naming positive (copy #{{ number }})"
